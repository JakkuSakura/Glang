package top.jackhack.glang.elements;

import java.util.ArrayList;

public class SpaceChar extends Element {
    @Override
    public int tryMatch(ArrayList<Element> elements, int index) {
        return 0;
    }
}
